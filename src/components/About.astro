---
---

<section class="page section" aria-labelledby="about-heading">
  <h2 id="about-heading">About</h2>
  <div class="prose">
    <p>
      I build products end-to-end — Go APIs, Next.js web apps, React Native
      mobile — rather than handing pieces to different specialists. That
      includes the product decisions as well as the implementation: what to
      build, and when a feature isn't worth the complexity it would add.
    </p>
    <p>
      ETP, the case study above, is what that looks like in practice: one
      person, three surfaces, shipped and running in production. I'm based in
      Singapore.
    </p>
  </div>
</section>

<style>
  .section {
    padding-block: var(--s-16);
    border-block-start: 1px solid var(--c-rule);
  }

  h2 { margin-block-end: var(--s-8); }

  .prose p + p {
    margin-block-start: var(--s-4);
  }
</style>
